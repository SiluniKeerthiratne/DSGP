const OutputPage = ({ prediction }) => {
  return (
      <div>
          <h3 className="text-white font-poppins font-semibold text-base">{prediction}</h3>
      </div>
  );
};

export default OutputPage;

